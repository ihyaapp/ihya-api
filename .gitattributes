# For every file, let Git auto-detect if it's text, and when it is, normalize line endings to LF (Unix-style) in the repo. This keeps the repo canonical regardless of what OS someone commits from.
* text=auto eol=lf

# Exception: .cmd files (like mvnw.cmd) always get CRLF, since Windows batch scripts expect that.
*.cmd text eol=crlf
