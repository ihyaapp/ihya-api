services:
  postgres:
    image: postgres:16
    container_name: ihya-postgres
    environment:
      POSTGRES_DB: ihya
      POSTGRES_USER: ihya
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: